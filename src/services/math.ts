export default class math{
    LiquidInvestment (a:number, b: number, c:number, d:number, e:number, f:number, g:number, h:number) {
        return a+b+c+d+e+f+g+h;
    }
    RealEsate(a:number, b: number, c:number, d:number, e:number, f:number, g:number){
        return a+b+c+d+e+f+g;
    }
    CoporateHolding(a:number, b: number){
        return a+b;
    }
    OtherAsset(a:number, b: number, c:number, d:number, e:number){
        return a+b+c+d+e;
    }
    Liabilities(a:number, b: number, c:number){
        return a+b+c;
    }
    ReservedEquity(a:number, b: number){
        return a+b;
    }
}